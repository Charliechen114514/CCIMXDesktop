/*
 * Hello! This file is auto generated by CMake, dont modified the
 * file directly, modify your desktop configure cmake is the best choice :)
 * Enjoy!
 */

#ifdef ARM_BUILD /* Is this special for ARM Build ? */
#define WALLPAPER_DEFAULT_DIRENT "@DEFAULT_DESKTOP_ARM_PLACES@"
#else
#define WALLPAPER_DEFAULT_DIRENT "@DEFAULT_DESKTOP_NONARM_PLACES@"
#endif

// root install path
#ifdef ARM_BUILD
#define _DESKTOP_ROOT_PATH "."
#else
#define _DESKTOP_ROOT_PATH "@CCIMX_ALL_RELEASE_DIR@"
#endif

// third party query default dirent
#define _EXTERNAPP_INSTALL_DIR "@CCIMX_EXTERNAL_APP_BASE_DIR@"

// defines the backligh files
// should be specified in template files
#define BACKLIGHT_BASE_PATH "@BACKLIGHT_BASE_PATH@"

// defines the topsidebar widget
#define TOPSIDEBAR_HEIGHT   @TOPSIDEBAR_HEIGHT@

// defines the topsidebar limits
#define TOPSIDEBAR_HEIGHT_MAX   60
#define TOPSIDEBAR_HEIGHT_MIN   30
