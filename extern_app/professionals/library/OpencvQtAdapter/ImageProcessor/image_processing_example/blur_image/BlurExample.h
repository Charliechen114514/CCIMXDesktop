#ifndef BLUREXAMPLE_H
#define BLUREXAMPLE_H

#include <QWidget>

namespace Ui {
/**
 * @brief Forward declaration of the UI class generated by Qt Designer.
 */
class BlurExample;
}

/**
 * @brief The BlurExample class demonstrates various blur processing methods
 * in a QWidget-based interface.
 *
 * It provides interactive testing and UI control for different types of blur
 * filters including median, Gaussian, mean, and bilateral filter blurs.
 */
class BlurExample : public QWidget {
    Q_OBJECT

    /**
     * @brief The minimum kernel size allowed for blur operations.
     */
    static constexpr unsigned short MIN_KER_SZ = 1;

    /**
     * @brief The maximum kernel size allowed for blur operations.
     */
    static constexpr unsigned short MAX_KER_SZ = 19;

public:
    /**
     * @brief Constructs the BlurExample widget with an optional parent.
     * @param parent The parent widget.
     */
    explicit BlurExample(QWidget* parent = nullptr);

    /**
     * @brief Destructor for BlurExample.
     */
    ~BlurExample();

    /**
     * @brief Entry point to perform the currently selected blur operation.
     */
    void process_blur();

private:
    /**
     * @brief Pointer to the auto-generated UI layout.
     */
    Ui::BlurExample* ui;

    /**
     * @brief Processes the median blur on the input image.
     */
    void process_medianblur();

    /**
     * @brief Processes the Gaussian blur on the input image.
     */
    void process_guassainblur();

    /**
     * @brief Processes the mean blur on the input image.
     */
    void process_meanblur();

    /**
     * @brief Processes the bilateral filter blur on the input image.
     */
    void process_filterblur();
};

#endif // BLUREXAMPLE_H
